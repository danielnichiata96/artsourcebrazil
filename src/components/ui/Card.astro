---
export interface Props {
  variant?: 'default' | 'bordered' | 'elevated';
  padding?: 'sm' | 'md' | 'lg';
  class?: string;
}

const { variant = 'default', padding = 'md', class: className = '', ...rest } = Astro.props;

const baseClasses = 'text-ink';

const variantClasses = {
  default: 'border-2 border-ink shadow-hard',
  bordered: 'border-thick border-ink',
  elevated: 'border-2 border-ink shadow-hard-lg',
};

const paddingClasses = {
  sm: 'p-4',
  md: 'p-6',
  lg: 'p-8',
};

const classes = `${baseClasses} ${variantClasses[variant]} ${paddingClasses[padding]} ${className}`;
---

<div class={classes} {...rest}>
  <slot />
</div>
