---
import Layout from '../layouts/Layout.astro';
import Card from '../components/ui/Card.astro';
import Button from '../components/ui/Button.astro';
import Link from '../components/ui/Link.astro';

const site = Astro.site ?? new URL('http://localhost:4321');
const canonicalUrl = new URL(Astro.url.pathname, site).href;
const seo = {
  title: 'Post a Job — Art Source Brazil',
  description:
    "Reach Brazil's top creative talent in Game Dev, 3D & Animation, and UI/UX. Post your remote role in minutes.",
  image: new URL('/images/og-default.svg', site).href,
  url: canonicalUrl,
};
// Safe fallbacks for envs so builds don't fail if not configured yet
const paymentLink = import.meta.env.PUBLIC_STRIPE_PAYMENT_LINK ?? '#';
const jobFormUrl = import.meta.env.PUBLIC_JOB_FORM_URL ?? '#';
---

<Layout seo={seo}>
  <main class="container mx-auto px-container py-12">
    <div class="mx-auto max-w-3xl text-center">
      <div class="inline-flex items-center gap-2 border-2 border-ink bg-white px-3 py-1 mb-6 shadow-hard-sm">
          <span class="w-2 h-2 rounded-full bg-brazil-green animate-pulse"></span>
          <span class="text-xs font-bold uppercase tracking-widest">Contratação Simplificada</span>
      </div>
      
      <div class="border-2 border-ink bg-white p-8 md:p-12 shadow-hard relative overflow-hidden">
        <div class="absolute top-0 left-0 w-full h-2 bg-brazil-hero"></div>
        
        <h1 class="font-display text-4xl md:text-5xl font-bold text-ink mb-6">Anunciar Vaga</h1>
        <p class="text-xl leading-relaxed text-neutral-600 mb-10">
          Seu anúncio fica 30 dias no ar e chega direto nos melhores talentos criativos do Brasil.
        </p>

        <div class="mb-10 bg-neutral-50 border-2 border-dashed border-neutral-300 p-8 rounded-lg">
          <div class="flex items-center justify-center gap-2 mb-2">
             <span class="text-5xl font-bold text-ink">$99</span>
             <span class="text-xl font-bold text-neutral-500 self-end mb-2">USD</span>
          </div>
          <p class="text-neutral-600 font-medium">
            Sua vaga fica 30 dias no ar pra uma comunidade focada em arte e games.
          </p>
        </div>

        <Button
          href={paymentLink}
          size="lg"
          variant="primary"
          class="w-full md:w-auto md:px-12"
          aria-label="Ir para pagamento"
        >
          Pagar e anunciar vaga
        </Button>

        <p class="mt-8 text-sm text-neutral-500 max-w-lg mx-auto">
          Depois do pagamento, você preenche um formulário rápido com os detalhes da vaga.
          Se não abrir automaticamente, é só <Link href={jobFormUrl} target="_blank" rel="noopener noreferrer" class="font-bold text-primary hover:underline">clicar aqui</Link>.
          Dúvidas? Manda email pra <Link href="mailto:artsourcebrazil@gmail.com" class="font-bold text-primary hover:underline">artsourcebrazil@gmail.com</Link>
        </p>
      </div>
    </div>
  </main>
</Layout>
