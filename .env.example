# ===============================================
# 🔐 SECURITY NOTICE
# ===============================================
# ⚠️ NEVER commit this file with real values to git!
# ⚠️ Variables starting with PUBLIC_ are exposed to the browser
# ⚠️ Variables without PUBLIC_ are server-side only (build time)
# 
# This project implements:
# ✅ XSS protection via DOMPurify sanitization
# ✅ CSP headers without 'unsafe-inline' for scripts
# ✅ No sensitive data exposed to client-side code



# ===============================================
# STRIPE PAYMENT CONFIGURATION
# ===============================================
# 🔒 SERVER-SIDE ONLY (used in API routes)
# Get your keys from: https://dashboard.stripe.com/test/apikeys
# ⚠️ NEVER expose the secret key to client-side code!
STRIPE_SECRET_KEY="sk_test_your_secret_key_here"

# 🌐 PUBLIC (exposed to browser - safe for public use)
# Publishable key for client-side Stripe.js (if needed in future)
PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_your_publishable_key_here"

# 🔒 SERVER-SIDE ONLY (webhook signature verification)
# Get this from Stripe CLI when testing locally: stripe listen --forward-to localhost:4321/api/stripe-webhook
# For production, get from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret_here"

# 🔒 SERVER-SIDE ONLY (product configuration)
# Create a Product in Stripe Dashboard: "Job Posting - 30 Days" at $99 USD
# Then create a Price for that product and paste the Price ID here (starts with price_)
# Get from: https://dashboard.stripe.com/test/products
STRIPE_PRICE_ID="price_your_price_id_here"

# 🌐 PUBLIC (exposed to browser - safe for public use)
# External job submission form URL (e.g., Tally, Google Forms, etc.)
# After payment, users will be redirected here to submit job details
# Used in: src/pages/post-a-job.astro, src/pages/post-a-job/success.astro
PUBLIC_JOB_FORM_URL="https://tally.so/r/your-form-id"

# ===============================================
# ANALYTICS (Optional)
# ===============================================
# 🌐 PUBLIC (exposed to browser - safe for public use)
# Plausible Analytics (optional)
# Set this to your domain (e.g., artsourcebrazil.vercel.app) to enable Plausible analytics
# Leave empty or remove if you don't want to use Plausible
# Used in: src/layouts/Layout.astro (conditionally loads Plausible script)
PUBLIC_PLAUSIBLE_DOMAIN=""

# 🌐 PUBLIC (exposed to browser - safe for public use)
# Newsletter subscription URL (Buttondown or similar service)
# This is the form action URL for newsletter signups
# Used in: src/layouts/Layout.astro (footer newsletter form)
PUBLIC_NEWSLETTER_SUBSCRIBE_URL="https://buttondown.email/api/emails/embed-subscribe/your-username"

# ===============================================
# 📊 VERCEL ANALYTICS (Automatic)
# ===============================================
# Vercel Analytics is automatically enabled in production (import.meta.env.PROD)
# No configuration needed - just enable it in your Vercel Dashboard
# Script loaded from: /_vercel/insights/script.js


# ===============================================
# SUPABASE CONFIGURATION (Required for job sync)
# ===============================================
# 🔒 SERVER-SIDE ONLY (used in scripts/sync-supabase.mjs, sync-greenhouse-to-supabase.mjs)
# Get your project URL from: https://supabase.com/dashboard/project/{project-id}/settings/api
SUPABASE_URL="https://your-project-id.supabase.co"

# Service Role Key (full access, bypasses RLS) - Use for scripts
# ⚠️ NEVER expose this to client-side code!
# Get from: https://supabase.com/dashboard/project/{project-id}/settings/api
# Scroll to "Project API keys" > "service_role" (secret)
SUPABASE_SERVICE_ROLE_KEY="your_service_role_key_here"

# 🌐 PUBLIC (exposed to browser - safe for client-side use)
# These are needed for the job posting form to save drafts from the browser
# Get from the same page as above
PUBLIC_SUPABASE_URL="https://your-project-id.supabase.co"
PUBLIC_SUPABASE_ANON_KEY="your_anon_key_here"

